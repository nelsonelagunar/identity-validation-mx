apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: identity-validation-ingress
  namespace: identity-validation
  labels:
    app: identity-validation-mx
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - api.identity-validation-mx.example.com
      secretName: identity-validation-tls
  rules:
    - host: api.identity-validation-mx.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: identity-validation-api
                port:
                  number: 8080